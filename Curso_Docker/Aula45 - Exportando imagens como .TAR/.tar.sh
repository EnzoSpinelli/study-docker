1. docker pull travelping/nettools
docker image --help
docker image save --help ( veremos que tem a opção de -o para salvar em um arquivo .tar )
docker image save travelping/nettools -o nettools.tar ( para salvar a imagem em um arquivo .tar )