Para instalar o docker compose, na maioria das distribuição isso ja vai vir instalado, mas caso não venha, você pode instalar com o seguinte comando:

1. apt search docker-compose
2. apt install docker-compose